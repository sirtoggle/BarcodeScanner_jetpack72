# EasyOCR support packages that are not supplied by the Jetson PyTorch image.
# OpenCV, torch, and torchvision must come from the JetPack-compatible image.
scipy>=1.11,<2
Pillow>=10,<13
scikit-image>=0.22,<1
python-bidi>=0.6,<1
PyYAML>=6,<7
Shapely>=2,<3
pyclipper>=1.3,<2
ninja>=1.11,<2
